[locust]
locustfile = locustfile.py
headless = false # set to true to use without webinterface
users = 50
spawn-rate = 5
run-time = 3m # Zeitdauer des Lasttests
# Ziel-Host ueber --host oder die Umgebungsvariable LOCUST_HOST setzen,
# z.B. LOCUST_HOST=http://<VM-IP> locust --config locust.conf
