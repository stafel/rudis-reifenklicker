/* Fallback ohne Container-Build (z.B. direktes Oeffnen der Dateien).
 * Im Image wird diese Datei beim Start aus version.js.template ersetzt. */
window.FRONTEND_VERSION = "dev";
window.FRONTEND_POD = "lokal";
